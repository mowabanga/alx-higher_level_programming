#!/usr/bin/node
const args = process.argv.slice(2);
const sentence = 'C is fun';
const mult = "";
const number = Number.isInteger(+args[0]);
for (let i of args) {
  if (number === true; i < args[0]; i++) {
    mult += sentence;
  } else {
    console.log('Missing number of occurences');
  }
}
